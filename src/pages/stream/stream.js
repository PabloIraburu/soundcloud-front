import React from 'react'
import "./stream.css"

const stream = () => {
    return (
        <div className="stream">
            <h1 className="stream-h1">Escucha los últimos posts de la gente a la que sigues:</h1>
            <div className="stream-p">
            <p>Tu stream está vacío en estos momentos. Utiliza Buscar o Inicio para encontrar creadores a los que seguir. Vuelve a tu stream para ver las pistas que están posteando.</p>
            </div>
            
        </div>
    )
}

export default stream;
